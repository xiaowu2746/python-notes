#leetcode练习
##defaultdict的使用//相比于dict，defaultdict会自动为不存在的键设置默认值0，且defaultdict使用哈希表，复杂度为O（1）
```python
###导入
from collections import defaultdict
###
  dic=defaultdict(int)
###对于一个字符串的储存
  for char in str :
    dic[char]+=1
  dic['a']+=1
  print(dic)//defaultdict(<class 'int'>, {'a': 1})
###哈希表

  
  
